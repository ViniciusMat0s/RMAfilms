Font files used by this project:

- `Aventa-Light.otf` for the `Aventa` family
- `bierika.otf` for the `Bierika` family used in CSS

The CSS in `app/globals.css` is configured to load these `.otf` files from `/public/fonts`.
